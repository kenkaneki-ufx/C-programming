/* ============================================================
   DAILY CODING TASK - DAY 12 (Dynamic Memory - beginner friendly)
   ============================================================

   QUESTION: Can you create an array at runtime, with a size
   you choose while the program is running?

   TASK (easy part - just follow the pattern):
   1. Ask the user: "How many numbers?" and scanf it into n
   2. Create the array: int *arr = malloc(n * sizeof(int));
      (include stdlib.h for malloc)
   3. Fill it with 1, 2, 3... n and print all of them
   4. Free the memory at the end: free(arr);

   BONUS (only if you feel ready - skip if not!):
   malloc returns NULL if it fails. Check for that and print
   "Out of memory!" instead of crashing.

   You write ALL the code below. Start from the skeleton.

   Stuck? Write below the code:
   // QUESTION: ...your question here...
   ============================================================ */

#include <stdio.h>
#include <stdlib.h>

void main() {

}
