/* ============================================================
   DAILY CODING TASK - DAY 11 (Searching - beginner friendly)
   ============================================================

   QUESTION: Can you find a number inside an array?

   TASK (easy part - Linear Search is just a loop):
   1. Make an array: int arr[6] = {10, 25, 30, 45, 50, 65};
   2. Ask the user for a number: scanf("%d", &x);
   3. Loop through the array and check each element
   4. Print "Found!" (and the position) or "Not found"

   BONUS (only if you feel ready - skip if not!):
   The array is already sorted - try Binary Search instead:
   check the middle element, then throw away half the array
   each time. Much faster!

   You write ALL the code below. Start from the skeleton.

   Stuck? Write below the code:
   // QUESTION: ...your question here...
   ============================================================ */

#include <stdio.h>

void main() {

}
